// const MAX_RETRIES = 5;
// const MAX_RETRIES = 10; // This will cause an error because MAX_RETRIES is a constant and cannot be reassigned.

//  const score = 100;
//  score = 100+1;//we cannot change the value of score as it is a constant variable
  const pi = 3.14;
  console.log(pi);
  //pi = 3.14159; // This will cause an error because pi is a constant and cannot be reassigned.    
  const prod_url = "https://api.example.com";
    console.log(prod_url);