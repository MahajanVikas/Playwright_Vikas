//Identifiers, Literals, Opetaors

var a=10;
a=20;
console.log(a);

//Variable name = identifier - a (name which is given to the container) 
//Variable value = literal - 10 (value which is stored in the container)
//= ->Operators
 var a1=10;
 console.log(a1);

 //Rules of identifiers
 //1. It can contain letters, digits, underscores, and dollar signs.
 //2. It must begin with a letter, underscore, or dollar sign.
 //3. It cannot be a reserved keyword in JavaScript (like var, let, const, function, etc.).
 //4. It is case-sensitive (e.g., myVariable and myvariable are different identifiers).
 //5. It should not contain spaces (e.g., my variable is invalid, but my_variable is valid).
 //6.special characters are not allowed except for underscores and dollar signs (e.g., my-variable is invalid, but my_variable is valid).
  var _myVariable = 10; // Valid identifier
  console.log(_myVariable);
   var $myVariable = 20; // Valid identifier
    console.log($myVariable);
    var myVariable1 = 30; // Valid identifier
    console.log(myVariable1);

    var name = "Vikas"; // Valid identifier
    console.log(name); 
    var NAME = "Mahajan"; // Valid identifier (case-sensitive)
    console.log(NAME);
    var π =3.14; // Valid identifier (Unicode character)
    console.log(π);

   