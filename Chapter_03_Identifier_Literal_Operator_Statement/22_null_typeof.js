let selecteditem = null;
let searchResult = null;

let declareonly


console.log(selecteditem); // Output: null
console.log(declareonly); // Output: undefined because declareonly is declared but not initialized, so it has the default value of undefined.
